@extends('layouts.app')

@section('title', 'My Bookings')
@section('page-title', 'My Bookings')

@section('sidebar-menu')
    <a href="{{ route('rep.dashboard') }}" class="nav-link">
        <i class="bi bi-speedometer2"></i> Dashboard
    </a>
    <a href="{{ route('rep.bookings.index') }}" class="nav-link active">
        <i class="bi bi-calendar-check"></i> My Bookings
    </a>
    <a href="{{ route('rep.bookings.create') }}" class="nav-link">
        <i class="bi bi-plus-circle"></i> New Booking
    </a>
    <a href="{{ route('rep.bookings.history') }}" class="nav-link">
        <i class="bi bi-clock-history"></i> History
    </a>
    <a href="{{ route('rep.profile.edit') }}" class="nav-link">
        <i class="bi bi-person-circle"></i> My Profile
    </a>
@endsection

@section('content')
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">
            <i class="bi bi-calendar-check"></i> All My Bookings
        </h5>
        <a href="{{ route('rep.bookings.create') }}" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> New Booking
        </a>
    </div>

    <div class="card-body">
        @if($bookings->count() > 0)
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Department</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Status</th>
                        <th>Created</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                @foreach($bookings as $booking)
                    <tr>
                        <td><code>#{{ $booking->id }}</code></td>

                        <td>
                            <i class="bi bi-hospital"></i>
                            {{ $booking->department->name }}
                            @if($booking->department->is_pharmacy_department)
                                <span class="badge bg-info ms-1">Pharmacy</span>
                            @endif
                        </td>

                        <td>{{ $booking->formatted_date }}</td>
                        <td>{{ $booking->formatted_time_slot }}</td>

                        {{-- STATUS --}}
                        <td>
                            <span class="badge bg-{{ $booking->status_color }} text-dark">

                                {{ $booking->status_text }}
                            </span>
                        </td>

                        <td>{{ $booking->created_at->format('M d, Y') }}</td>

                        {{-- ACTIONS --}}
                        <td>
                            @if($booking->status === 'pending')
                                <button
                                    type="button"
                                    class="btn btn-sm btn-outline-danger"
                                    data-bs-toggle="modal"
                                    data-bs-target="#cancelModal{{ $booking->id }}">
                                    <i class="bi bi-x-circle"></i> Cancel
                                </button>

                            @elseif(in_array($booking->status, ['rejected', 'cancelled']))
                                <button
                                    type="button"
                                    class="btn btn-sm btn-outline-info"
                                    data-bs-toggle="modal"
                                    data-bs-target="#reasonModal{{ $booking->id }}">
                                    <i class="bi bi-info-circle"></i> View Reason
                                </button>

                            @else
                                <span class="text-muted small">No actions</span>
                            @endif
                        </td>
                    </tr>

                    {{-- ================= MODALS ================= --}}

                    {{-- Cancel Modal --}}
                    @if($booking->status === 'pending')
                    <div class="modal fade" id="cancelModal{{ $booking->id }}" tabindex="-1">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title">Cancel Booking</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </div>

                                <form method="POST" action="{{ route('rep.bookings.cancel', $booking) }}">
                                    @csrf
                                    <div class="modal-body">
                                        <div class="alert alert-warning">
                                            <i class="bi bi-exclamation-triangle"></i>
                                            Are you sure you want to cancel this booking?
                                        </div>

                                        <p><strong>Department:</strong> {{ $booking->department->name }}</p>
                                        <p><strong>Date:</strong> {{ $booking->formatted_date }}</p>
                                        <p><strong>Time:</strong> {{ $booking->formatted_time_slot }}</p>

                                        <div class="mb-3">
                                            <label class="form-label">Cancellation Reason</label>
                                            <textarea
                                                name="cancellation_reason"
                                                class="form-control"
                                                rows="3"
                                                required></textarea>
                                        </div>
                                    </div>

                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                            Close
                                        </button>
                                        <button type="submit" class="btn btn-danger">
                                            <i class="bi bi-x-circle"></i> Cancel Booking
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                    @endif

                    {{-- Reason Modal (Rejected / Cancelled) --}}
                    @if(in_array($booking->status, ['rejected', 'cancelled']))
                    <div class="modal fade" id="reasonModal{{ $booking->id }}" tabindex="-1">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header bg-secondary text-white">
                                    <h5 class="modal-title">Booking Reason</h5>
                                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                </div>

                                <div class="modal-body">
                                    <p><strong>Department:</strong> {{ $booking->department->name }}</p>
                                    <p><strong>Date:</strong> {{ $booking->formatted_date }}</p>
                                    <p><strong>Time:</strong> {{ $booking->formatted_time_slot }}</p>
                                    <hr>
                                    <p class="mb-0">
                                        {{ $booking->rejection_reason ?? $booking->cancellation_reason ?? 'No reason provided.' }}
                                    </p>
                                </div>

                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                        Close
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    @endif

                @endforeach
                </tbody>
            </table>
        </div>

        <div class="mt-3">
            {{ $bookings->links() }}
        </div>

        @else
            <div class="text-center py-5">
                <i class="bi bi-calendar-x text-muted" style="font-size: 4rem;"></i>
                <h5 class="mt-3 text-muted">No Bookings Yet</h5>
                <p class="text-muted">You haven't created any bookings.</p>
                <a href="{{ route('rep.bookings.create') }}" class="btn btn-primary mt-2">
                    <i class="bi bi-plus-circle"></i> Create Your First Booking
                </a>
            </div>
        @endif
    </div>
</div>
@endsection
